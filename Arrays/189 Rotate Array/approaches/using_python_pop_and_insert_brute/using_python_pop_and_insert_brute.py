# Problem: 189. Rotate Array
# Approach: Using python pop and insert brute
# Language: python3
# Time: O(n*k)
# Space: O(1)

        k=k%n
        for _ in range(k):
            num=nums.pop()
        n=len(nums)
        """
        Do not return anything, modify nums in-place instead.
        """
    def rotate(self, nums: List[int], k: int) -> None:
            nums.insert(0,num)
class Solution:
            print(nums)
        return nums
